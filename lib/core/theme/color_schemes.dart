import 'package:flutter/material.dart';

class AppColorSchemes {
  static const light = ColorScheme.light();
  static const dark = ColorScheme.dark();
  static const amoled = ColorScheme.dark(
    surface: Colors.black,
  );
}
